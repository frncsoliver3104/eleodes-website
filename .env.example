# Leave empty for static hosting, which serves /public/releases/app-release.apk directly.
# Set this only when using the optional Express download service.
VITE_DOWNLOAD_URL=https://your-backend-domain.example/api/download
